# Draft Submissions

The website writes new draft entries here first.

These folders are intentionally separate from `shaders/` so contributors can use the browser flow without mutating the canonical corpus directly. Maintainers should review each draft, promote accepted entries into `shaders/`, and keep the final history in normal git commits.
